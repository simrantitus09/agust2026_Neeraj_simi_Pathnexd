Jenkins - basic CI/CD Pipeline

pipeline {
    agent any

    stages { 
         
         stage('Build') { 
            steps {
                echo 'Building project'
            }
         } 

         stage('Test') {
            step { 
                echo 'Running tests'
            }
         }

         stage('Deploy') {
            step {
                echo 'Deploying application'
            }
         }
    }
}


GitLab CI/CD — Basic Pipeline

stages:
  - Build
  - test
  - deoloy

build:
   stage: build
   script:
     - echo "Building project"

test:
   stage: deploy
   script:
     - echo "Deploying application"


Ansible — Install and Configure Nginx

- name:Install and configure Nginux
  host: all
  become: yes
  task:
    - name: Install nginx
      yum:
        name: nginx
        state: present
    - name: configure nginx
      template:  
        src: nginx.conf.j2
        dest: /etc/nginx/nginx.conf
    -name: Start nginx service
     service:
        name: nginx
        state: started
        enable: yes


Terraform — EC2 with Security Group

resource "aws_security_group" "example" {
  name        = "example_security_group"
  description = "Allow SSH and HTTP"
  ingress {
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  ingress {
    from_port   = 80
    to_port     = 80
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }
}


Docker
# Apache Container

FROM ubuntu:22.04
RUN apt update && apt install apache2 -y
RUN echo "Hello Pathnex" > /var/www/html/index.html
CMD ["apachectl", "-D", "FOREGROUND"]

# Bash Real Path
/var/www/html
